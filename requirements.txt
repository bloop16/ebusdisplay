# Waveshare 2.13" e-Paper HAT V4 – Raspberry Pi Zero
# Waveshare library: install separately via install.sh (not on PyPI)

# Display
Pillow>=10.0.0
RPi.GPIO>=0.7.1
spidev>=3.6

# PiSugar 3 (direkt per I2C, kein Daemon)
smbus2>=0.4.3

# Web-UI (Konfiguration, läuft parallel)
Flask>=3.0.0
